# Workspace Template Layout

This directory is the reusable workspace template for book-specific execution.

Actual runs should use a book-local workspace such as:

- `journey-to-the-west/workspace/`

- `state/`: current mutable working state
- `checkpoints/`: per-chapter frozen artifacts
- `outputs/`: book-level deliverables
- `reports/`: audits and stage reports
