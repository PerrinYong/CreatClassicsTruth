# Checkpoints Directory

Create one subdirectory per chapter, for example:

- `001/`
- `002/`
- `023/`

Each checkpoint should contain the chapter truth, retelling, audit report, graph artifacts, and the frozen state copy used to advance the runloop.
