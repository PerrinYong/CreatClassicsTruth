# Journey To The West Workspace

这是《西游记》的小说工作空间。

目录说明：

- `xiyouji_txt/`：原文章节源文本
- `workspace/`：本书的实际运行工作区

本目录下的 `workspace/` 承载：

- 当前工作状态
- 章节 checkpoint
- 全书输出
- 阶段报告

根目录 `workspace/` 仅作为模板使用，不作为本书的实际运行目录。
