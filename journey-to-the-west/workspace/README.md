# Book Workspace Layout

This is the active workspace for the book stored in this directory.

- `state/`: current mutable working state for this book
- `checkpoints/`: per-chapter frozen artifacts for this book
- `outputs/`: finalized book-level deliverables
- `reports/`: audits and stage reports for this book

The root-level `workspace/` is a template only.
